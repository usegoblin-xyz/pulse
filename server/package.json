{
  "name": "pulse-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pulse's key-holding brain: turns form fields + profile into a safe fill plan.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\"",
    "start": "node dist/src/server.js",
    "dev": "node --watch dist/src/server.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
