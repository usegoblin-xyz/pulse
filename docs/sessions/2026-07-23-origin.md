# Origin — how Pulse was born (2026-07-21 → 2026-07-23)

Pulse started inside the Kara-3 repo and was split out on 2026-07-23. This is
the record of that work, moved here so the Kara repo stays Kara-only.

## The beam masters (`masters/`)

The character (a Bane-like figure the user generated with Seedance) got Kara's
"materialization beam" treatment via the user's Higgsfield account, ~53 credits
total, identity checked by frame extraction at every stage:

- Source frame media_id `380f4234-aa48-4a38-8c04-2c8d9e2a2e75`
- Still pass (nano-banana-pro 4K image-edit, background/lighting only — never
  regenerate the face): jobs `4b384a0d…` (A) and `5d86e7e4…` (B, chosen) →
  `masters/bane-beam-still-4k.png`
- Motion pass (Seedance image-to-video): job `15ba7079…` (720p), upscaled to
  2K via bytedance upscale: job `cf6468ee…` → `masters/bane-beam-static-2k.mp4`
- Palindrome bake (recipe in `docs/beam-effect.md`) →
  `masters/bane-beam-loop.mp4` = `site/pulse-beam-loop.mp4` (20 s seamless)

Still-rebuild was chosen over video-restyle because the source clip's lighting
drifted and restyling risks identity.

## The landing page (`site/`)

Visual sibling of Kara's page. Final reviewed state: Gulax font (Velvetyne,
SIL OFL) on the "Pulse" wordmark only at 52px, tagline and lore in system font
(15px / 12.5px), left column flush-left. Lore copy is the user's own (Errol
Vance). Verified by headless-Chrome screenshot, then deployed to GitHub Pages.

## The persona and architecture

`PERSONA.md` was written in the same session: behaviors (form filling, guided
navigation, driven navigation), voice rules inherited from Kara, and the hard
guardrails. The extension architecture and four milestones are in `README.md`.
